import React from "react";
import ReactDOM from "react-dom/client";
import StudyAI from "./StudyAI"; // the file from canvas

import "./index.css"; // optional: keep or create basic css

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StudyAI />
  </React.StrictMode>
);
