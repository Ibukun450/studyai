import React from "react";
import ReactDOM from "react-dom/client";
import EasylearnAI from "./EasylearnAI"; // the file from canvas

import "./index.css"; // optional: keep or create basic css

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EasylearnAI />
  </React.StrictMode>
);
